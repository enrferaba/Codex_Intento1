[project]
name = "sandbox-runner"
version = "0.1.0"
description = "Runner de sandbox"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = []
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
