{
  "title": "FERIA Overview",
  "panels": [
    {
      "type": "graph",
      "title": "Query latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(router_latency_seconds_bucket[5m])) by (le))"
        }
      ]
    }
  ]
}
