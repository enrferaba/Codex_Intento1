[project]
name = "feria-sdk"
version = "0.1.0"
description = "SDK de clientes"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"
