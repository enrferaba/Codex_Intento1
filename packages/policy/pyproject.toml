[project]
name = "feria-policy"
version = "0.1.0"
description = "Validadores de políticas"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"
